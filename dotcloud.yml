## nginx + uwsgi
#api:
#    type: python
#    config:
#        python_version: v2.7

api:
    type: custom
    buildscript: ./build.sh
    ports:
        www: http

cache:
    type: redis

bandwidth:
    type: redis